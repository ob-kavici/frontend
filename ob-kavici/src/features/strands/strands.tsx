import { Game } from '@/types/games';
import React from 'react';

const Strands: React.FC<Game> = () => {
    return (
        <div>
            <h1>Strands Game</h1>
        </div>
    );
};

export default Strands;