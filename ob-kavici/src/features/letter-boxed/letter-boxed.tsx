import { Game } from '@/types/games';
import React from 'react';

const LetterBoxed: React.FC<Game> = () => {
    return (
        <div>
            <h1>Letter Boxed Game</h1>
        </div>
    );
};

export default LetterBoxed;